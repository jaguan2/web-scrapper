// Color tokens for Media Scraper, matching the personal-portfolio look:
// warm off-white canvas, near-black ink, a single hot-pink accent, and flat
// dark buttons. Light-only, like the portfolio.

export const colors = {
  background: '#f7f7f2', // warm off-white canvas
  surface: '#ffffff', // cards / panels
  surfaceMuted: '#f1f1ea', // inset wells (input, dropzone interior)
  ink: '#1e1e24', // near-black text + primary buttons
  inkMuted: '#6c6c74', // secondary text
  border: '#e4e4dd', // hairline borders
  borderStrong: '#d3d3c9',
  accent: '#ff004f', // portfolio's signature hot pink
  accentSoft: '#ffe5ed', // faint accent wash (focus fills, selected tints)
}
